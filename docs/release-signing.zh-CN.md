# 供应链签名与校验

## 发布签名建议

- npm 发布时启用 provenance（`npm publish --provenance`）。
- 发布产物应包含来源哈希与提交信息。

## 镜像签名建议

- Docker 镜像建议使用 Cosign 进行签名。
- 在部署前验证签名完整性。

## 校验流程

1. 校验发布标签与提交哈希一致。
2. 校验产物哈希与构建日志一致。
3. 校验依赖清单与锁文件一致。
